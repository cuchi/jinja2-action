name: 'Jinja2 Action'
description: 'Use the Jinja2 templating engine'
inputs:
  template:
    required: true
  output_file:
    required: true
  strict:
    required: false
    default: false
  variables:
    required: false
  data_file:
    required: false
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'sliders'
  color: 'red'
